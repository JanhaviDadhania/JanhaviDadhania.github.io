// MDX components available in tool pages. Intentionally smaller than the
// blog/post components map — tool pages are plain text + images, with
// references via markdown footnotes.

import Figure from '@/components/Figure.astro';

export const toolComponents = { Figure };
